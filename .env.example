# Resend email API — https://resend.com
# Sign up, verify your domain (or use onboarding@resend.dev for testing),
# then create an API key and set FROM_EMAIL to a verified sender address.
RESEND_API_KEY=re_xxxxxxxxxxxx
FROM_EMAIL=onboarding@resend.dev
